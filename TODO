Ordered by priority

Backend:
  Convert global variables to a cache interface
  DB functions should be through an interface

  Better error handling, especially when irc disconnects
  Actually handle DB errors


Frontend:
  Sort by CurrKPM
  Animate cells to represent new rankings
  First place cell design
  Overall design

  Leaderboard pages
  FAQ page
  About page


CURRENTLY BROKEN:
  Sometimes unmarshalling json loses the url and status fields...


Startup Process:
  updateDB -> getTopStreams -> insertDB -> updateDB
  api request -> requestJSON -> queryDB
